name: CI

on: 
    push:

jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - name: Retriew code
              uses: actions/checkout@v6
            
            - name: Install uv
              uses: astral-sh/setup-uv@v7
              with:
                python-version: "3.12"
            
            - name: Install dependencies
              run: uv sync --locked